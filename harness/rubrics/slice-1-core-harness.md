# Frozen Rubric — Slice 1: Core harness / CLI / evidence

**Status:** FROZEN  
**Frozen at:** 2026-07-13T21:30:00Z  
**Branch:** `phase-5-remediation`  
**Sources:** setter A (`cefcde7e`) + setter B (`49ff159e`); orchestrator merge  
**Scope:** `scripts/project_cli.py`, `scripts/evidence.py`, `tests/test_project_cli.py`, `tests/test_evidence_manifest.py`, `evidence/example/**`, `PLAN.md`/`STATUS.md`/`ISSUES.md` JSON authority, `harness/**`, `BREAK_FIX_LOG.md`  
**Out of scope:** Jenkins/compose/Jenkinsfile, deploy/rollback/verify_deployment, portfolio docs, Phase 7 lanes

**Scoring rule:** All must-haves must PASS. Average judge score must be ≥ 9.5/10 to advance. Judges score only against this frozen artifact.

---

## Must-have (any fail = slice fail)

| ID | Check | Pass criteria |
|---|---|---|
| S1-M01 | `python scripts/project_cli.py validate state --json` | exit 0; `passed: true`; `checks.state: []` |
| S1-M02 | `python scripts/project_cli.py validate skills --json` | exit 0; `checks.skills: []` |
| S1-M03 | `python scripts/project_cli.py validate phase-1 --json` | exit 0; `passed: true` |
| S1-M04 | `python -m pytest tests/test_project_cli.py tests/test_evidence_manifest.py -q -o addopts=` | all tests pass (0 failed) |
| S1-M05 | `python scripts/project_cli.py evidence example --json` | `valid: true`, `errors: []` |
| S1-M06 | `python scripts/evidence.py validate --release-id example` | `valid: true`, `errors: []` |
| S1-M07 | Files `evidence/example/events.jsonl` + `manifest.json` | exist; schema_version `6.0`; claim_boundary contains `local-only`; events non-empty JSONL |
| S1-M08 | Phase auth: `python scripts/project_cli.py phase 9 --dry-run --json` | exit 6 (`EXIT_HUMAN`); error mentions not authorized |
| S1-M09 | Secret rejection covered by `tests/test_evidence_manifest.py` (secrets test) | test passes; append rejects secret-like payloads |
| S1-M10 | Append-only + derived-manifest integrity covered by evidence tests | append-only + non-derived summary failure tests pass |
| S1-M11 | Digest / commit identity consistency covered by evidence tests | digest mismatch tests pass |
| S1-M12 | Production claim fail-closed covered by evidence tests | missing production-chain / rollback-or-first-release tests pass |
| S1-M13 | `harness/SLICES.md` + this frozen rubric path exist | Slice 1 row points at `harness/rubrics/slice-1-core-harness.md` |
| S1-M14 | `BREAK_FIX_LOG.md` exists with slice partition + table header | Timestamp/Slice/Break/Fix/Evidence/Result columns present |
| S1-M15 | Slice-1-scoped ruff clean: `python -m ruff check scripts/project_cli.py scripts/evidence.py tests/test_project_cli.py tests/test_evidence_manifest.py` | 0 errors |

## Needed for 9/10+

| ID | Check | Pass criteria |
|---|---|---|
| S1-9-01 | `current_phase ≤ authorized_through_phase`; no live-cloud/AWS claim in `verified_baseline` | consistent; claim boundary honest |
| S1-9-02 | Resume conflict: verified task not resumable | `test_verified_task_cannot_resume` passes |
| S1-9-03 | Unimplemented validate scopes (`evidence`,`container`,`github`,`jenkins`,`promotion`,`security`) return EXIT_HUMAN | not silent pass |
| S1-9-04 | Example events secret-scan clean under validate | no secrets-detected error on example |
| S1-9-05 | Recovery/rollback derivation tests pass | rollback_target / self-ref rejection covered |
| S1-9-06 | Dual CLI paths agree on example validity | project_cli evidence vs evidence.py validate both valid |

## Needed for 10/10

| ID | Check | Pass criteria |
|---|---|---|
| S1-10-01 | PLAN task states vs STATUS “phases 2–8 complete” narrative reconciled or explicitly documented as residual | no silent contradiction without note |
| S1-10-02 | Missing events log cannot pass on manifest alone | dedicated test passes |
| S1-10-03 | Gate fields byte-equal between derived and example manifest | `_gate_fields` match |
| S1-10-04 | BREAK_FIX_LOG records Slice 1 judge rounds with scores | at least one scored round logged |

## Nice-to-have

| ID | Check | Pass criteria |
|---|---|---|
| S1-N01 | `PROJECT_C_ROOT` honored | documented or tested |
| S1-N02 | Mechanical rubric scoring script | optional |
| S1-N03 | revision fields synced across PLAN/STATUS/ISSUES | optional |

## Local verification commands (Slice 1)

```powershell
python scripts/project_cli.py validate state --json
python scripts/project_cli.py validate phase-1 --json
python scripts/project_cli.py evidence example --json
python -m pytest tests/test_project_cli.py tests/test_evidence_manifest.py -q -o addopts=
python -m ruff check scripts/project_cli.py scripts/evidence.py tests/test_project_cli.py tests/test_evidence_manifest.py
```

Do not edit this file unless a newly discovered blocker affects security, integrity, or acceptance credibility.
