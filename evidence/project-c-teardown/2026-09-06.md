# Project C protected teardown evidence — 2026-09-06

## Change gate

- Repair PR: [#12](https://github.com/nathanielecon/local-first-governed-cicd/pull/12)
- Merged commit: `661770b552a509f4e134a2cc4760d7d2286925e2`
- Required hosted checks: Python quality, Security scans, Container contract, and Jenkinsfile contract all passed.
- Initial execution: [GitHub Actions run 34049187998](https://github.com/nathanielecon/local-first-governed-cicd/actions/runs/34049187998)
- Idempotent verification execution: [GitHub Actions run 34064646733](https://github.com/nathanielecon/local-first-governed-cicd/actions/runs/34064646733)
- Authority: protected `teardown` environment, explicit reviewer approval, short-lived GitHub OIDC, and verified non-root role.

## Sanitized result

The dry-run and apply discovery agreed:

- ECS service: absent before apply.
- Application load balancer: absent before apply.
- Target group: present before apply, then deleted successfully.
- ECS cluster: deleted successfully.
- Designated security group: the protected workflow returned the sanitized code `DependencyViolation`.

A subsequent non-root, read-only dependency check found that an unused Project C
service security group referenced the designated load-balancer group. Both
groups had zero attached network interfaces; the service group had no inbound
security-group references. The service group was therefore deleted first, then
the designated group. Independent lookups confirmed that both were absent.

The idempotent verification run confirmed that the service, load balancer, and
target group were already absent while authenticating through the exact
protected-environment OIDC subject. A follow-up inventory covered all 17 enabled
AWS regions and reported zero matching active ECS clusters or services, load
balancers, target groups, or Project C security groups, with zero regional read
errors.

The run log was scanned after completion. It contained no AWS account ID, live resource ARN, security-group ID, subnet ID, or VPC ID. The only ARN-like text was the static regular expression used to validate the assumed-role format.

## Preservation boundary

The script and its parameter-bound role did not grant delete operations for
ECR, CloudWatch Logs, VPCs/subnets, KMS, S3, or repository evidence. No
operation in either protected run targeted those categories.

A separate non-root read-only inventory after teardown confirmed one retained
Project C ECR repository, one retained Project C CloudWatch log group, the
default VPC and its six default subnets, and readable KMS-alias and S3-bucket
inventories. The retained identity could not call `DescribeImages` or
`ListImages`, so this record does not claim a fresh post-teardown lookup of the
image digest. The immutable digest remains documented in the repository's
earlier live-validation evidence, and neither cleanup identity had ECR delete
permission.

## Temporary-access cleanup

After resource verification:

- The dedicated Project C teardown role and its sole inline policy were deleted.
- The temporary Project A and Project C user policies were deleted.
- The temporary `AWSCloudShellFullAccess` attachment was removed while the
  existing `ContinuityOpsAgentReadOnly` attachment was preserved.
- The account-wide GitHub OIDC provider was not modified or deleted.
- The teardown-only workflow, protected secret, and environment were retired
  after this evidence passed the repository's normal reviewed pull-request gate.
