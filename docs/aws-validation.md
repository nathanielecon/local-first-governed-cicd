# Optional AWS Validation

Status: deferred and not implemented.

When authorized, add Terraform for a minimal ECR repository and ECS/Fargate staging service, authenticate with short-lived OIDC, publish the unchanged image contract, reuse smoke/evidence/rollback semantics, and document cost plus teardown. Live evidence must precede any AWS implementation claim.

